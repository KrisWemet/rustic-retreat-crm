name: Database Checks

on:
  push:
  pull_request:

jobs:
  database:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Verify migrations and database behavior
        run: bash supabase/tests/run.sh
